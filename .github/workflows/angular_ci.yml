on:
  push

jobs:
  example_job:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./DYTHG
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v4
      - run: npm run test
